#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {
	double mas[100];
	double h,c;
	int i;
	printf("Vvedite chislo h:");
	scanf("%lf", &h);
	
	for (i=0, c =1; c < h; i++, c = c + 0.1){
		mas[i] = c;
		printf("mas [%d] = %.1lf\n", i,c);
	}
	return 0;
}
