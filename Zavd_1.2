#include <stdio.h>
#include <stdlib.h>
#include <time.h>


int main(int argc, char *argv[]) {
	int mas[100];
	int i;
	
	srand(time(0));
	for(i = 0; i<10; i++){
		mas[i] = (rand() %200) -100;
		printf ("mas [%d] = %d\n", i, mas [i]);
	}
	
	return 0;
}
