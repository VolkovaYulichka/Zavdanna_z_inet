#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main(int argc, char *argv[]) {
	double mas[100];
	int i;
	double h;
	printf ("Vvedite chislo h:");
	scanf ("%lf", &h);
	
	srand(time(0));
	for (i = 0; i < 10; i++){
		mas[i] = ((float)rand()/RAND_MAX)*h;
		printf (" mas [%d] = %lf\n", i, mas [i]);
}
	
	
	return 0;
}
