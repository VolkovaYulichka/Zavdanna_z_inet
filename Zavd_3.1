#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int poisk(int *mas, int n, int x);

int main()
{
	int mas[300], i, k;
	int *adres;
	
	srand(time(0));
		for(i = 0; i < 10; i++) {
		mas[i] = - 100 + (rand() % 200);
		printf("mas[%d] = %d\n", i, mas[i]);
	}
		printf("Vvedite chislo: ");
	scanf("%d", &k);
		if((i = poisk(mas, 10, k)) >= 0)
		printf("Vashe chislo %d v mas[%d].\n", k, i);
	else
		printf("Vashe chislo %d ne naydeno.\n", k);
	
	return 0;
}

int poisk(int *mas, int n, int k)
{
	int i;
	for(i = 0; i < n; i++) {
		if(mas[i] == k)
			return i;
	}
		return -1;
}
