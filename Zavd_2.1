#include <stdio.h>
#include <stdlib.h>

int main (int argc, char *argv[]){
	int mas[100];
	int h,i;
	

	printf ("Vvedite chislo h:" );
	scanf ("%d", &h);
	for (i = 0; i < h; i++){
		mas [i] = i + 1;
		printf ("mas [%d] = %d\n",i,mas [i]);
		
	}

	return 0;
}
