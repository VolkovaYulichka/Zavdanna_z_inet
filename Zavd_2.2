#include <stdio.h>
#include <stdlib.h>

int main (int argc, char *argv[]){
	int mas[100];
	int a,b,i,n;

	printf ("Vvedite chislo a:" );
	scanf ("%d", &a);
	printf ("Vvedite chislo b:" );
	scanf ("%d", &b);
	for (i = 0, n = a; n < b; i++, n++){
		mas [i] = n;
		printf ("mas [%d] = %d\n",i,n);
		
	}
	return 0;
}
